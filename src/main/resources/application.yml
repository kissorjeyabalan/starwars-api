spring:
  profiles: prod
  jpa:
    hibernate:
      ddl-auto: create-drop
  properties:
    hibernate:
      temp:
        use_jdbc_metadata_defaults: false
server:
  port: ${PORT:5000}

properties
---

spring:
  profiles: test
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create-drop